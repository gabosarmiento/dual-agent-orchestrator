name: Validate MVP
on:
  push:
    branches: [main, feat/local-mvp, feat/persistent-workspace]
  pull_request:
    branches: [main, feat/local-mvp]
jobs:
  test:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - run: npm test
      - run: npm run check
